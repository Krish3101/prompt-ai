[project]
name = "prompt-ai"
version = "0.1.0"
description = "Prompt AI (Simplified MVP)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.3",
     "requests>=2.32.3",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.3",
]
